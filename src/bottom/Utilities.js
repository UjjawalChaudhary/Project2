import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Utilities = () => {
  return (
    <View style={{backgroundColor:'white', alignItems:'center', marginTop:200}}>
    <Text style={{ alignSelf:'center', marginTop:200, fontSize:50}}>
    Utilities</Text>
    </View>
  )
}

export default Utilities

const styles = StyleSheet.create({})